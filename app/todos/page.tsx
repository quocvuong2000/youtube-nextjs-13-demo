import TodosList from "./TodosList";

type Props = {};

const Todos = ({}: Props) => {
  return (
    <div>
      <h1>This is where the todo list will be listed</h1>
    </div>
  );
};

export default Todos;
